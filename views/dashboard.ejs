<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Dashboard</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --brand-primary: #5A67D8;
            --brand-secondary: #805AD5;
            --text-primary: #1A202C;
            --text-secondary: #4A5568;
            --background-light: #F7FAFC;
            --container-bg: #FFFFFF;
            --border-color: #E2E8F0;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--background-light);
            margin: 0;
            color: var(--text-primary);
        }

        .header {
            background-color: var(--container-bg);
            padding: 1rem 2rem;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header h1 {
            font-size: 1.25rem;
            margin: 0;
        }

        .header a {
            color: var(--brand-primary);
            text-decoration: none;
            font-weight: 500;
        }

        .main-content { padding: 2rem; }
        .container { max-width: 1100px; margin: 0 auto; }
        .grid { display: grid; grid-template-columns: 1fr; gap: 1.5rem; }
        @media (min-width: 900px) { .grid { grid-template-columns: 1.2fr 1fr; } }

        .card {
            background: var(--container-bg);
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            margin-bottom: 2rem;
        }

        .card h2 {
            font-size: 1.5rem;
            margin-top: 0;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 1rem;
            margin-bottom: 1.5rem;
        }

        .status {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            color: white;
            font-weight: 600;
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
        .status.active { background-color: #38A169; }
        .status.incomplete { background-color: #E53E3E; }

        form { display: flex; flex-direction: column; gap: 1rem; }
        label { font-weight: 600; color: var(--text-secondary); }
        input[type="text"], input[type="url"] {
            padding: 0.75rem 1rem;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            font-size: 1rem;
            font-family: 'Inter', sans-serif;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }
        input:focus {
            outline: none;
            border-color: var(--brand-primary);
            box-shadow: 0 0 0 3px rgba(90, 103, 216, 0.3);
        }

        .btn { display:inline-flex; align-items:center; gap:.5rem; }
        button {
            align-self: flex-start;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            background: var(--brand-primary);
            color: white;
            font-weight: 700;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.2s ease, transform 0.1s ease;
        }
        button:hover { background: #4C51BF; }
        button:active { transform: scale(0.98); }
        .btn-secondary { background:#EDF2F7; color:#2D3748; font-weight:600; }
        .btn-secondary:hover { background:#E2E8F0; }

        .link-box {
            background-color: var(--background-light);
            padding: 1rem;
            border-radius: 8px;
            font-family: monospace;
            word-wrap: break-word;
            border: 1px solid var(--border-color);
        }

        .kpis { display:grid; grid-template-columns: repeat(3,1fr); gap: 1rem; }
        .kpi { background:#F8FAFF; border:1px solid var(--border-color); border-radius:12px; padding:1rem; text-align:center; }
        .kpi .label { color: var(--text-secondary); font-size: .85rem; }
        .kpi .value { font-size: 1.4rem; font-weight: 800; margin-top: .25rem; }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
        }
        th, td {
            text-align: left;
            padding: 1rem;
            border-bottom: 1px solid var(--border-color);
        }
        tbody tr:nth-child(odd){ background:#FAFBFF; }
        thead th { position: sticky; top: 0; z-index:1; }
        .table-wrap { overflow:auto; border:1px solid var(--border-color); border-radius:12px; }

        .toast { position: fixed; right: 16px; bottom: 16px; background:#1A202C; color:#fff; padding:.75rem 1rem; border-radius:10px; opacity:0; transform: translateY(6px); transition: all .2s ease; }
        .toast.show { opacity:1; transform: translateY(0); }
        th {
            background-color: #F7FAFC;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.75rem;
            letter-spacing: 0.05em;
            color: var(--text-secondary);
        }
        td {
             vertical-align: top;
        }
        .rating-star {
            color: #FBBF24;
            font-weight: 700;
        }
    </style>
</head>
<body>
    <header class="header">
        <h1><%= business.businessName %></h1>
        <a href="/logout">Log Out</a>
    </header>

    <main class="main-content">
      <div class="container">
        <div class="grid">
          <div class="card">
            <h2>Subscription</h2>
            <p>
                Your current status is:
                <span class="status <%= business.subscriptionStatus %>">
                    <%= business.subscriptionStatus %>
                </span>
            </p>
            <% if (business.subscriptionStatus !== 'active') { %>
                <p>To unlock all features, please subscribe for $49.99/month.</p>
                <form action="/create-checkout-session" method="POST">
                    <input type="hidden" name="_csrf" value="<%= (typeof csrfToken !== 'undefined' ? csrfToken : '') %>">
                    <button type="submit">Subscribe Now</button>
                </form>
            <% } else { %>
                <form action="/billing-portal" method="POST" style="margin-top: 1rem;">
                    <input type="hidden" name="_csrf" value="<%= (typeof csrfToken !== 'undefined' ? csrfToken : '') %>">
                    <button type="submit">Manage Billing</button>
                </form>
            <% } %>
          </div>

          <% if (business.subscriptionStatus === 'active') { %>
          <div class="card">
                <h2>Settings & Rating Link</h2>
                <form action="/update-settings" method="POST">
                    <input type="hidden" name="_csrf" value="<%= (typeof csrfToken !== 'undefined' ? csrfToken : '') %>">
                    <label for="googlePlaceId">Your Google Place ID:</label>
                    <input type="text" id="googlePlaceId" name="googlePlaceId" value="<%= business.googlePlaceId || '' %>" placeholder="Enter your Google Place ID here" required>
                    <button type="submit">Save Settings</button>
                </form>
                <br>
                <label>Your Unique Rating Link:</label>
                <div class="link-box" id="publicLink"><%= appUrl %>/rate/<%= user.uid %></div>
                <div class="btns" style="display:flex; gap:.5rem; margin-top:.5rem;">
                  <button class="btn btn-secondary" id="copyBtn" type="button">Copy link</button>
                  <a class="btn btn-secondary" id="openLink" href="<%= appUrl %>/rate/<%= user.uid %>" target="_blank" rel="noopener">Open link</a>
                </div>
                <div style="margin-top:1rem;">
                  <div style="font-weight:600; color: var(--text-secondary); margin-bottom:.25rem;">QR Code</div>
                  <div id="qrcode" aria-label="QR code for your rating link"></div>
                </div>
          </div>

          <% if (typeof analytics !== 'undefined') { %>
          <div class="card">
            <h2>Insights</h2>
            <div class="kpis">
              <div class="kpi"><div class="label">Total feedback</div><div class="value"><%= analytics.total %></div></div>
              <div class="kpi"><div class="label">Average rating</div><div class="value"><%= analytics.avg %></div></div>
              <div class="kpi"><div class="label">5★ conversions</div><div class="value"><%= analytics.conversions %></div></div>
            </div>
            <p style="margin-top:1rem; color: var(--text-secondary);">Distribution: 1★ <%= analytics.counts[1] %> • 2★ <%= analytics.counts[2] %> • 3★ <%= analytics.counts[3] %> • 4★ <%= analytics.counts[4] %> • 5★ <%= analytics.counts[5] %></p>
          </div>
          <% } %>

          <div class="card" style="grid-column: 1 / -1;">
                <h2>Customer Feedback</h2>
                <% if (feedback.length > 0) { %>
                    <div class="table-wrap">
                    <table>
                        <thead>
                            <tr>
                                <th>Rating</th>
                                <th>Name</th>
                                <th>Contact</th>
                                <th>Details</th>
                                <th>Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% feedback.forEach(item => { %>
                                <tr>
                                    <td><span class="rating-star"><%= item.rating %> ★</span></td>
                                    <td><%= item.name %></td>
                                    <td>
                                        <%= item.email %>
                                        <% if (item.phone) { %><br><%= item.phone %><% } %>
                                    </td>
                                    <td><%= item.feedback || '-' %></td>
                                    <td><%= new Date(item.createdAt).toLocaleDateString() %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                    </div>
                <% } else { %>
                    <p>You haven't received any feedback yet.</p>
                <% } %>
          </div>
          <% } %>
        </div>
      </div>
    </main>
    <div class="toast" id="toast">Link copied</div>
    <script>
      (function(){
        const btn = document.getElementById('copyBtn');
        if (!btn) return;
        const toast = document.getElementById('toast');
        btn.addEventListener('click', async () => {
          const link = document.getElementById('publicLink').innerText.trim();
          try {
            await navigator.clipboard.writeText(link);
            toast.classList.add('show');
            setTimeout(()=>toast.classList.remove('show'), 1600);
          } catch (_) {
            alert('Copy failed');
          }
        });
      })();
    </script>
    <!-- Lightweight QR code generator; allowed in dev where CSP is disabled -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <script>
      (function(){
        var box = document.getElementById('qrcode');
        if (!box) return;
        var link = document.getElementById('publicLink').innerText.trim();
        try { new QRCode(box, { text: link, width: 160, height: 160 }); } catch(e) {}
      })();
    </script>
</body>
</html>

