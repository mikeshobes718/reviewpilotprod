<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin • Reviews &amp; Marketing</title>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/header') %>
  <div class="container" style="max-width:1100px;margin:24px auto;">
    <h1>Admin</h1>
    <table class="table">
      <thead><tr><th>Business ID</th><th>Name</th><th>Email</th></tr></thead>
      <tbody>
        <% items.forEach(function(item){ %>
          <tr><td><%= item.id %></td><td><%= item.businessName || '' %></td><td><%= item.email || '' %></td></tr>
        <% }) %>
      </tbody>
    </table>
  </div>
  <%- include('partials/footer') %>
</body>
</html>


