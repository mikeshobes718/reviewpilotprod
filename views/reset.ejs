<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset password • Reviews &amp; Marketing</title>
    <%- include('partials/head') %>
  </head>
  <body class="auth-bg">
    <%- include('partials/header') %>
    <div class="auth-grid">
      <div class="auth-left auth-photo"></div>
      <div class="auth-right">
        <div class="base-card auth-card dark-card">
          <div class="brand" style="text-align:center"><img src="/logo.svg" alt="Reviews &amp; Marketing" style="height:32px;width:auto" /></div>
          <div class="form-container">
          <h2>Reset your password</h2>
          <p class="sub-headline">Enter your email and we'll send you a link to get back into your account.</p>
            <% if (typeof error !== 'undefined' && error) { %>
              <div class="error"><%= error %></div>
            <% } %>
            <% if (!sent) { %>
            <form action="/reset-password" method="POST">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <div class="form-field">
                <label for="email">Email</label>
                <input id="email" class="input" type="email" name="email" placeholder="you@business.com" required />
              </div>
              <div class="button-container"><button class="btn-primary primary-action" type="submit">Send reset email</button></div>
            </form>
            <% } else { %>
            <p class="sub-headline">Please check your inbox for a password reset email.</p>
            <% } %>
          </div>
        </div>
      </div>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
