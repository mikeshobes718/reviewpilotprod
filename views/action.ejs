<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a new password • Reviews &amp; Marketing</title>
    <%- include('partials/head') %>
    <style>
      * { box-sizing: border-box; }
      body { font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif; background: var(--bg); margin: 0; }
      .base-card { width: 360px; margin: 40px auto; }
      label { font-size: 13px; color: var(--muted); font-weight: 600; }
      p.sub { color: var(--muted); margin: 0 0 18px; font-size: 14px; }
      .error { background:#FEF2F2; color:#991B1B; border:1px solid #FECACA; padding:10px 12px; border-radius:8px; font-size:14px; margin-bottom:8px }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="auth-grid">
      <div class="auth-right">
        <div class="base-card">
          <div class="brand"><div class="badge">RM</div></div>
          <h1>Create a new password</h1>
          <% if (typeof error !== 'undefined' && error) { %>
            <div class="error"><%= error %></div>
          <% } %>
          <form action="/auth/action" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <input type="hidden" name="oobCode" value="<%= oobCode %>">
            <div>
              <label>Email</label>
              <input class="input" type="email" value="<%= email %>" disabled />
            </div>
            <div>
              <label>New Password</label>
              <input class="input" type="password" name="newPassword" required />
            </div>
            <button class="btn-primary" type="submit" style="margin-top:16px">Save password</button>
          </form>
        </div>
      </div>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>


