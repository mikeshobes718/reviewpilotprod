<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot password • Reviews &amp; Marketing</title>
    <%- include('partials/head') %>
  </head>
  <body class="auth-bg">
    <%- include('partials/header') %>
    <div class="auth-grid">
      <div class="auth-left auth-photo"></div>
      <div class="auth-right">
        <div class="base-card auth-card dark-card">
          <div class="brand" style="text-align:center"><img src="/logos/Landscape-Photoroom.png" alt="Reviews &amp; Marketing" /></div>
          <div class="form-container">
            <h2>Forgot your password?</h2>
            <p class="sub-headline">Enter your email and we'll send you a reset token.</p>
            <% if (typeof sent !== 'undefined' && sent) { %>
              <div class="noticeBanner">If an account exists, a reset link has been sent.</div>
            <% } %>
            <div id="fp-msg" style="display:none" class="noticeBanner"></div>
            <form action="/auth/forgot-password" method="POST">
              <div class="form-field">
                <label for="fp-email">Email</label>
                <input id="fp-email" class="input" type="email" name="email" placeholder="you@business.com" required />
              </div>
              <div class="button-container"><button id="fp-btn" class="btn-primary primary-action" type="submit">Send Reset Link</button></div>
            </form>
          </div>
        </div>
      </div>
    </div>
    <%- include('partials/footer') %>
  </body>
  </html>


