<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Set new password • Reviews &amp; Marketing</title>
    <%- include('partials/head') %>
  </head>
  <body class="auth-bg">
    <%- include('partials/header') %>
    <div class="auth-grid">
      <div class="auth-left auth-photo"></div>
      <div class="auth-right">
        <div class="base-card auth-card dark-card">
          <div class="brand" style="text-align:center"><img src="/logos/Landscape-Photoroom.png" alt="Reviews &amp; Marketing" /></div>
          <div class="form-container">
            <h2>Create a new password</h2>
            <p class="sub-headline">Enter and confirm your new password.</p>
            <form action="/auth/reset-password" method="POST">
              <input type="hidden" name="token" value="<%= typeof token !== 'undefined' ? token : '' %>">
              <div class="form-field">
                <label for="rpw-new">New Password</label>
                <input id="rpw-new" class="input" type="password" name="newPassword" required />
              </div>
              <div class="form-field">
                <label for="rpw-confirm">Confirm New Password</label>
                <input id="rpw-confirm" class="input" type="password" name="confirmNewPassword" required />
              </div>
              <div class="button-container"><button id="rpw-btn" class="btn-primary primary-action" type="submit">Reset Password</button></div>
            </form>
          </div>
        </div>
      </div>
    </div>
    <%- include('partials/footer') %>
    
  </body>
  </html>


