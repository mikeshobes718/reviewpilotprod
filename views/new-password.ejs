<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Set New Password • Reviews &amp; Marketing</title>
    <%- include('partials/head') %>
    <style>
      * { box-sizing: border-box; }
      body { font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif; background: var(--bg); margin: 0; }
      .base-card { width: 360px; margin: 40px auto; }
      label { font-size: 13px; color: var(--muted); font-weight: 600; }
      p.sub { color: var(--muted); margin: 0 0 18px; font-size: 14px; }
      .hint { color: var(--muted); font-size: 12px; margin-top: 6px; }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="auth-grid">
      <div class="auth-left">
        <div>
          <h1>Set your new password</h1>
          <p>Choose a strong password to secure your account.</p>
        </div>
      </div>
      <div class="auth-right">
        <div class="base-card">
          <div class="brand"><div class="badge">RM</div></div>
          <h1>Set New Password</h1>
          <p class="sub">Finish setting up your account</p>
          <% if (typeof error !== 'undefined' && error) { %>
            <div class="error"><%= error %></div>
          <% } %>
          <form action="/new-password" method="POST">
            <input type="hidden" name="_csrf" value="<%= (typeof csrfToken !== 'undefined' ? csrfToken : '') %>">
            <div>
              <label>Email</label>
              <input class="input" type="email" value="<%= email || '' %>" disabled>
            </div>
            <div>
              <label>New Password</label>
              <input class="input" type="password" name="newPassword" placeholder="Create a password" required>
              <div class="hint">Minimum 8 characters, including uppercase, lowercase, number, and symbol.</div>
            </div>
            <div>
              <label>Confirm New Password</label>
              <input class="input" type="password" name="confirmPassword" placeholder="Re-enter your password" required>
            </div>
            <button class="btn-primary" type="submit" style="margin-top:24px">Save and Log In</button>
          </form>
        </div>
      </div>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
