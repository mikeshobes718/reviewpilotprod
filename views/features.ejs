<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/header') %>
  <main class="container section" style="max-width:1100px;margin:0 auto;">
    <!-- Hero -->
    <section class="section" style="text-align:center">
      <h1 class="hero-title" style="font-family:Inter,Manrope,sans-serif;font-weight:800;font-size:clamp(42px,7vw,64px)">Powerful Features to Grow Your Reputation.</h1>
      <p class="body-text" style="color:var(--muted);max-width:760px;margin:8px auto 0">Everything you need to request reviews, filter feedback, and see clear analytics—all in one place.</p>
      <div class="mt-3">
        <% if (user) { %>
          <a href="/dashboard" class="btn primary">Go to Dashboard</a>
        <% } else { %>
          <a href="/signup" class="btn primary">Get started</a>
        <% } %>
      </div>
    </section>

    <!-- Feature Spotlight 1 -->
    <section class="section">
      <div class="feature-spotlight">
        <img alt="Automated requests" class="product-shot" src="https://via.placeholder.com/1000x600.png?text=Automated+Requests" style="width:100%;height:auto" />
        <div>
          <div class="eyebrow">Automated requests</div>
          <h2 class="hero-title" style="font-size:34px">Never Miss an Opportunity to Get a 5‑Star Review</h2>
          <p class="body-text" style="color:var(--muted)">Send high‑converting SMS and email requests after every visit. Schedule reminders automatically and keep your brand consistent.</p>
        </div>
      </div>
    </section>

    <!-- Feature Spotlight 2 (alternate) -->
    <section class="section">
      <div class="feature-spotlight">
        <div>
          <div class="eyebrow">Private feedback</div>
          <h2 class="hero-title" style="font-size:34px">Catch Issues Before They Go Public</h2>
          <p class="body-text" style="color:var(--muted)">Route 1‑4★ experiences to a private form first so your team can respond quickly, resolve problems, and protect your reputation.</p>
        </div>
        <img alt="Private feedback" class="product-shot" src="https://via.placeholder.com/1000x600.png?text=Private+Feedback" style="width:100%;height:auto" />
      </div>
    </section>

    <!-- Final CTA -->
    <section class="containedSection">
      <div style="text-align:center">
        <h2 class="hero-title" style="font-size:36px">Ready to turn customers into reviews?</h2>
        <% if (user) { %>
          <a href="/dashboard" class="btn primary" style="margin-top:8px">Go to Dashboard</a>
        <% } else { %>
          <a href="/signup" class="btn primary" style="margin-top:8px">Get started</a>
        <% } %>
      </div>
    </section>
  </main>
  <%- include('partials/footer') %>
</body>
</html>

